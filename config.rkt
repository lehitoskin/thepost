; config.rkt
; configuration file for thepost

; sending connection port
(define sender 119)

; receiving connection port
(define receiver 119)

; port to connect to the server
(define port-number 119)

; auth information defaults
(define username "anon")
(define password "anon")